# A Kanban Board for drupal.org issues

Use it here:
http://lullabot.github.io/drupalorg-issues/

or to run locally, install a http server with ```npm install``` inside this directory and then:
```npm run start```
